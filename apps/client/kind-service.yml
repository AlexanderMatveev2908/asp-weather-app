apiVersion: v1
kind: Service
metadata:
  name: asp-job-application-tracker-client
spec:
  type: NodePort
  selector:
    app: asp-job-application-tracker-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
      nodePort: 30081
