apiVersion: v1
kind: Service
metadata:
  name: asp-weather-app-server
spec:
  type: NodePort
  selector:
    app: asp-weather-app-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
